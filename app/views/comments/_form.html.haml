%h4 Add comment:
=form_for @comment, url: {movie_id: @movie.id, controller: 'comments', action: 'create' }do |f|
  .form-group
    =f.text_area :content, placeholder: 'Add your comment', class: 'form-control', rows: 5
    =f.submit class:'btn btn-primary btn-block'
